# Digit Recognition by Hand Movement
Uses the webcam to detect hand movement via an object to create an image of the digit. Thereafter, that image is used to identify the digit using Machine Learning.
